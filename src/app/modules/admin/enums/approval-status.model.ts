// This is an EJS template. It generates the enum file.
export enum ApprovalStatus {
  
  APPROVED = 'APPROVED',
  
  DECLINED = 'DECLINED',
  
  PENDING = 'PENDING',
  
  TO_REVIEW = 'TO_REVIEW',
  
  FAILED = 'FAILED'
  
}
